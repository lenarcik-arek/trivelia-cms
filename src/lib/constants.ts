/** Default coin budget for new quiz stops */
export const DEFAULT_COIN_BUDGET = 20;

/** Default expiry duration for new quiz stops (in hours) */
export const DEFAULT_EXPIRY_HOURS = 24;

/** Default map center (Warsaw) */
export const DEFAULT_MAP_CENTER: [number, number] = [52.2297, 21.0122];

/** Default map zoom level */
export const DEFAULT_MAP_ZOOM = 13;

/** Zoom level when focusing on a single quiz stop */
export const FOCUSED_MAP_ZOOM = 17;

/** Map tile URL */
export const MAP_TILE_URL = "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png";

/** Map tile attribution */
export const MAP_TILE_ATTRIBUTION = "&copy; OpenStreetMap";
